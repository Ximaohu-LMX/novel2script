export default function App() {
  return (
    <main className="app-shell">
      <section className="intro-panel">
        <p className="eyebrow">Novel2Script</p>
        <h1>React + Vite scaffold</h1>
        <p>
          This baseline keeps the app runnable while the product features are
          introduced through focused follow-up commits.
        </p>
      </section>
    </main>
  )
}
